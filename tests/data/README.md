Test fixture notes:
- This folder provides tiny FASTQ/FASTA/GTF files for UI and smoke tests.
- When both `sample.fastq` and `sample2.fastq` are mounted at `/input`,
  the UI auto-fills two conditions (`control`, `stz`) to speed local testing.
